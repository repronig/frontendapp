/**
 * Member application — query key surface (Phase 3 feature module convention).
 */
import { queryKeys } from '@/lib/queryKeys';

export const memberApplicationQueryKeys = {
  me: queryKeys.memberApplicationMe,
} as const;
